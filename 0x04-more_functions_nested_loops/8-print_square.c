#include "main.h"

/**
 * print_square - prints square
 * @size: integer arg
 */

void print_square(int size)
{
	int k = 0;
	int t = 0;

	while (k < size)
	{
		for (t = 0; t <= size; t++)
			_putchar('#');
		k++
	}
}
