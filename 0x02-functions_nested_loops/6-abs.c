#include "main.h"

/**
 * _abs - computes absolute value
 * @int: takes in an integer arguments
 * Return: 0 means success
 */
int _abs(int)
{
	int k;

	if (k >= 0)
		_putchar(k);
	else
		{
			k  = -1 * k;
			_putchar(z);
		}
}
