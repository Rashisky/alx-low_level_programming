#include "main.h"

/**
 * main - checks for lowercase character
 * Return - 1 means Success, 0 means otherwise
 */
int main(void)
{
	char u = 'c';

	if (int_islower(u))
		return (1);
	else
		return (0);
}
