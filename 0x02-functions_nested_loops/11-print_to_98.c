#include <main.h>

/**
 * main - prints all natural numbers
 * Return: Always 0 Success
 */
int main(void)
{
	int n;

	print_to_98(n)
	{
		for (n = '0'; n <= '98'; n++)
		{
			putchar(n);
			putchar(',');
			putchar(' ');
		}
	}
	return (0);
}

	
