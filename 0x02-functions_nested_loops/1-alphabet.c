#include "main.h"

/**
 * main - prints alphabets
 * Return: 0 means Success
 */
int main(void)
{
	char i;

	print_alphabet(i)
	{
		for (i = 'a'; i <= 'z'; i++)
			_putchar(i);
	_putchar('\n');
	}
	return (0);
}
